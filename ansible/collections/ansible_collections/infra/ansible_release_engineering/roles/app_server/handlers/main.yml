---
# handlers file for infra.ansible_release_engineering.app_server

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart app
  ansible.builtin.systemd_service:
    name: app
    state: restarted
...
